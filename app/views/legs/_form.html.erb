<%= form_with(model: leg, local: true) do |form| %>
  <% if leg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leg.errors.count, "error") %> prohibited this leg from being saved:</h2>

      <ul>
      <% leg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :origin %>
    <%= form.text_field :origin, id: :leg_origin %>
  </div>

  <div class="field">
    <%= form.label :destination %>
    <%= form.text_field :destination, id: :leg_destination %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :leg_username %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
