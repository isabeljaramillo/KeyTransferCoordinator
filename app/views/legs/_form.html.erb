<%= form_with(model: leg, local: true) do |form| %>
  <% if leg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leg.errors.count, "error") %> prohibited this error from being saved:</h2>

      <ul>
      <%leg.errors.full_messages.each do |message| %>
      <li><%= message %>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div class = "field">
      <%= form.label :origin %>
      <%= form.text_field :origin, id: origin %>
    </div>

    <div class = "field">
      <%= form.label :destination %>
      <%= form.text_field :destination, id: destination %>
    </div>

    <div class = "field">
      <%= form.label :user_id %>
      <%= form.text_field :user_id,  id: user_id %>
    </div>

    <div class = "field">
      <%= form.label :trip_id %>
      <%= form.text_field :trip_id, id: trip_id %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>

<% end %>
